# Backend API base URL (no trailing slash, no /api/v1 — that's added automatically)
VITE_API_URL=http://localhost:4000

# Google Sign-In client ID from Google Cloud Console (see SETUP_GUIDE.md, step 4)
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
